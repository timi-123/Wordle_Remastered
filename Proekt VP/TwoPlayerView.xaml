<UserControl x:Class="Proekt_VP.TwoPlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Proekt_VP"
             xmlns:controls="clr-namespace:Proekt_VP.Controls"
             mc:Ignorable="d"
             Focusable="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="Margin" Value="0,6"/>
            <Setter Property="MinWidth" Value="160"/>
            <Setter Property="FontSize" Value="15"/>
        </Style>

        <Style x:Key="HpBar" TargetType="ProgressBar">
            <Setter Property="Foreground" Value="#4CAF50"/>
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Height" Value="22"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsHpLow}" Value="True">
                    <Setter Property="Foreground" Value="#FB8C00"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsHpCritical}" Value="True">
                    <Setter Property="Foreground" Value="#E53935"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PlayerPanel" TargetType="Border">
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Opacity" Value="0.5"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsMyTurn}" Value="True">
                    <Setter Property="BorderBrush" Value="#1E88E5"/>
                    <Setter Property="Background" Value="#F2F8FF"/>
                    <Setter Property="Opacity" Value="1.0"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TimerText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="44"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#212121"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsTimerLow}" Value="True">
                    <Setter Property="Foreground" Value="#E53935"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="White">

        <!-- Phase: choose time cap -->
        <StackPanel x:Name="TimeCapPanel" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="2 PLAYER BATTLE" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,4"/>
            <TextBlock Text="Time per guess" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,14"/>
            <Button Style="{StaticResource PrimaryButton}" Click="TimeCap10Click">10 seconds — Frantic</Button>
            <Button Style="{StaticResource PrimaryButton}" Click="TimeCap20Click">20 seconds — Standard</Button>
            <Button Style="{StaticResource PrimaryButton}" Click="TimeCap30Click">30 seconds — Relaxed</Button>
        </StackPanel>

        <!-- Phase: Player 1 word entry -->
        <StackPanel x:Name="P1EntryPanel" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Width="360">
            <TextBlock Text="PLAYER 1" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="Choose 5 secret words for Player 2" FontSize="15" HorizontalAlignment="Center" Margin="0,0,0,12" TextWrapping="Wrap" TextAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBox x:Name="P1WordInput" Width="200" FontSize="18" CharacterCasing="Upper" MaxLength="5" KeyDown="P1WordInput_KeyDown"/>
                <Button Content="Add" Margin="8,0,0,0" Padding="14,4" Click="P1AddWordClick"/>
            </StackPanel>
            <ItemsControl ItemsSource="{Binding P1ChosenWords}" Margin="0,10,0,0" HorizontalAlignment="Center">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,2"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Text="{Binding WordEntryError}" Foreground="Red" HorizontalAlignment="Center" Margin="0,6" MinHeight="18"/>
            <Button x:Name="P1ConfirmButton" Content="Confirm words ▶" Style="{StaticResource PrimaryButton}"
                    HorizontalAlignment="Center" IsEnabled="{Binding P1EntryComplete}" Click="P1ConfirmClick"/>
        </StackPanel>

        <!-- Phase: hand-off -->
        <StackPanel x:Name="HandoffPanel" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Pass the device to Player 2" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,6"/>
            <TextBlock Text="Player 1's words are hidden. No peeking!" FontSize="15" HorizontalAlignment="Center" Margin="0,0,0,16"/>
            <Button Content="I'm Player 2 — Continue ▶" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Center" Click="HandoffContinueClick"/>
        </StackPanel>

        <!-- Phase: Player 2 word entry -->
        <StackPanel x:Name="P2EntryPanel" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" Width="360">
            <TextBlock Text="PLAYER 2" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center"/>
            <TextBlock Text="Choose 5 secret words for Player 1" FontSize="15" HorizontalAlignment="Center" Margin="0,0,0,12" TextWrapping="Wrap" TextAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBox x:Name="P2WordInput" Width="200" FontSize="18" CharacterCasing="Upper" MaxLength="5" KeyDown="P2WordInput_KeyDown"/>
                <Button Content="Add" Margin="8,0,0,0" Padding="14,4" Click="P2AddWordClick"/>
            </StackPanel>
            <ItemsControl ItemsSource="{Binding P2ChosenWords}" Margin="0,10,0,0" HorizontalAlignment="Center">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,2"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <TextBlock Text="{Binding WordEntryError}" Foreground="Red" HorizontalAlignment="Center" Margin="0,6" MinHeight="18"/>
            <Button x:Name="P2ConfirmButton" Content="Start Battle ⚔" Style="{StaticResource PrimaryButton}"
                    HorizontalAlignment="Center" IsEnabled="{Binding P2EntryComplete}" Click="P2ConfirmClick"/>
        </StackPanel>

        <!-- Phase: battle -->
        <Grid x:Name="BattlePanel" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,8,0,0">
                <TextBlock Text="{Binding TurnText}" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock Style="{StaticResource TimerText}" Text="{Binding TimerText}"/>
            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource PlayerPanel}" DataContext="{Binding Player1}">
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <Grid Margin="0,4">
                            <ProgressBar Style="{StaticResource HpBar}" Minimum="0" Maximum="{Binding MaxHP}" Value="{Binding HP}"/>
                            <TextBlock Text="{Binding HP, StringFormat='{}{0} HP'}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <controls:WordleBoardControl IsEnabled="{Binding IsInputEnabled}"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Width="2" Background="LightGray" Margin="6,0"/>

                <Border Grid.Column="2" Style="{StaticResource PlayerPanel}" DataContext="{Binding Player2}">
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                        <Grid Margin="0,4">
                            <ProgressBar Style="{StaticResource HpBar}" Minimum="0" Maximum="{Binding MaxHP}" Value="{Binding HP}"/>
                            <TextBlock Text="{Binding HP, StringFormat='{}{0} HP'}" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Grid>
                        <controls:WordleBoardControl IsEnabled="{Binding IsInputEnabled}"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <Button Content="← Back to Menu" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10" Padding="10,5" Click="BackButton_Click"/>
    </Grid>
</UserControl>
